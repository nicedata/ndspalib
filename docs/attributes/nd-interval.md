# `nd-interval`

This attribute defines a poll interval in milliseconds. 

## Format

The format to use is **`nd-interval="ms"`**, for example `nd-interval="3000"` (3s).

Default : 10000 (10s). 

Intervals below 1s are set to 1000 (1s).

## Used in

:white_check_mark: `nd-poll`

